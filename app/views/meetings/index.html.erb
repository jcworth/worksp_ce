<h1>All meetings</h1>

<% @meetings.each do |meeting| %>
  <li><%= link_to "Location: #{meeting.location}", meeting_path(meeting)%>
  <p>Date: <%= meeting.date %></p></li>
<% end %>

<%= link_to "Create a new Worksp_ce", new_meeting_path %>
