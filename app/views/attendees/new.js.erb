const newAttendance = document.querySelectorAll('#new-attendance-render > a')

// newAttendance.addEventListener('click', (e) => {
//   newAttendance[0].insertAdjacentHTML('afterbegin', "<%= escape_javascript(render 'new') %>")
// });

newAttendance[0].insertAdjacentHTML('afterbegin', "<%= escape_javascript(render 'new') %>")
